# Similarity Search

## My understanding
Similarity search finds results that are semantically close to a query instead of matching exact words.

## Why it matters
It makes searches smarter and more flexible, especially for AI applications.

## Example
Searching “fast similarity search at scale” still found results related to vector databases even though the exact phrase was not in the article.